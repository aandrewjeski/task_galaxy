<%= form_for @tasklist do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name, required: true %><br>

  <%= f.label :description %>:
  <%= f.text_area :description %><br>

  <%= f.submit %>
<% end %>
